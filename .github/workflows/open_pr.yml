# crea entorno rama

on:
  pull_request_target:
    types:
      - opened
      
jobs:
   crea-entorno:
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo Se ha creado la PR            
            echo crear entorno BRANCH ${{ github.ref_name }}
 
