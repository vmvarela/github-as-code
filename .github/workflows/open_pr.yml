on:
  pull_request_target:
    types:
      - opened
      
jobs:
   create-environment:
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo The PR is opened            
            echo create BRANCH environment
 
