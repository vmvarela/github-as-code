on:
  pull_request_target:
    types:
      - opened
      
jobs:
   Create-Feature-Env:
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo Se ha creado la PR            
            echo crear entorno BRANCH ${{ github.head_ref }}
 
