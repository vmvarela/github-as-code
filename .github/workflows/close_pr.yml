# destruye entorno rama

on:
  pull_request_target:
    types:
      - closed

jobs:
  destruye-entorno:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo Se ha mergeado a main la PR
        echo eliminar entorno BRANCH ${{ github.head_ref }}

